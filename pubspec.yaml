name: an_lifecycle_cancellable
description: "After merging Lifecycle and Cancellable, the toolkit provided in flutter"
version: 0.0.4
homepage: https://github.com/aymtools/lifecycle_cancellable/
issue_tracker: https://github.com/aymtools/lifecycle_cancellable/issues

topics:
  - lifecycle
  - anlifecycle
  - cancel
  - cancellable
  - cancel-token

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cancellable: ^2.0.0
  anlifecycle: ^2.0.2

dev_dependencies:

